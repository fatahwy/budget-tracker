{
    "crons": [
        {
            "path": "/api/cron/ping-db",
            "schedule": "*/10 * * * *",
            "headers": {
                "authorization": "Bearer ${CRON_SECRET}"
            }
        }
    ]
}